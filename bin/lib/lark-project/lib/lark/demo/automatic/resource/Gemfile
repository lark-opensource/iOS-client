# frozen_string_literal: true

base_gemfile_path = File.join(File.dirname(__FILE__), 'AutoGenerated/BaseGemfile')
eval_gemfile(base_gemfile_path) if File.exist?(base_gemfile_path)

# you can use ref: "sha" to specify commit dependency
gem 'lark-project', git: 'git@code.byted.org:lark/ios-lark-project-mirror.git', branch: 'develop'
