//
// Created by bytedance on 2021/5/29.
//

#include <string>

/**
 * Script的额外字段
 */
static const std::string EXTRA_VAL_SCRIPT = "script_value";
/**
 * 和场景对齐方式
 */
static const std::string EXTRA_TAG_ALIGN_MODE_SCENE = "script_align_scene";
/**
 * 右对齐方式
 */
static const std::string EXTRA_VAL_ALIGN_MODE_SCENE_RIGHT = "align_scene_right";
/**
 * 左对齐方式
 */
static const std::string EXTRA_VAL_ALIGN_MODE_SCENE_LEFT = "align_scene_left";

static const std::string EXTRA_VAL_ALIGN_MODE_SCENE_BOTH = "align_scene_both";

static const std::string EXTRA_VAL_ALIGN_MODE_SCENE_NONE = "align_scene_none";




static const std::string EXTRA_VAL_ALIGN_MODE_SCENE_PADDING_START = "script_val_align_mode_scene_padding_start";

static const std::string EXTRA_VAL_ALIGN_MODE_SCENE_PADDING_END = "script_val_align_mode_scene_padding_end";



/**
 * 作用在整个场景下
 */
static const std::string EXTRA_VAL_APPLY = "apply_scene";

static const std::string EXTRA_KEY_BUSINESS = "business";

static const std::string EXTRA_CLIP_POS = "extra_clip_pos";

/**
 * 片头
 */
static const std::string EXTRA_CLIP_AT_HEAD = "script_clip_at_head";

/**
 * 片尾
 */
static const std::string EXTRA_CLIP_AT_TAIL = "script_clip_at_tail";
/*
 * 素材标志（用户添加）
 */
static const std::string EXTRA_TAG_MATERIAL = "script_material";
/*
 * 预设素材标志（模板自带）
 */
static const std::string EXTRA_TAG_DEFAULT_MATERIAL = "script_default_material";
