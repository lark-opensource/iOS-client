//
// Generated by FetchIcon.rb
//
// Resource version: <%= version %>
// Update time: <%= Time.now.strftime("%Y/%m/%d %H:%M") %>
//

import UIKit

extension UDIcon {

    /// 当前 UDIcon 所使用的资源包版本
    /// - NOTE: 最新 UDIcon 版本参考飞书 “Universe Design icons 发车群”
    public static var resourceVersion: String { "<%= version %>" }
}

// swiftlint:disable file_length
extension UDIcon {
<%# ignore numerous minor requests -- focus on iconNameHash %>
<% iconNames = iconNameHash.keys %>
<% iconNames.sort! %>
<% iconNames.each do |iconName| %>

    /// icon name: <%= iconName %>
    public class var <%= iconName %>: UIImage {
        return UDIcon.getIconByKey(.<%= iconName %>)
    }
<% end %>
}

// MARK: - Deprecated Icons

extension UDIcon {
<%# ignore numerous minor requests -- focus on deprecatedIconHash %>
<% depIconNames = deprecatedIconHash.keys %>
<% depIconNames.sort! %>
<% depIconNames.each do |name| %>

    @available(*, deprecated, renamed: "<%= deprecatedIconHash[name] %>")
    public class var <%= name %>: UIImage {
        return UDIcon.getIconByKey(.<%= deprecatedIconHash[name] %>)
    }
<% end %>
}
// swiftlint:enable file_length