//
//  DocsNetworkError+UI.swift
//  SKFoundation
//
//  Created by lijuyou on 2020/5/24.
//  


import SKFoundation
import EENavigator
import SKUIKit
import UniverseDesignToast


extension DocsNetStateMonitor {

    /*
    /// 注意，如果网络不可用，则返回true
    ///
    /// - Parameter notReachableToast: 如果在网络不可用时需要toast, 注意国际化文案
    /// - Returns: 注意，如果网络不可用，则返回true
    public class func checkNetStateIsNotReachable(notReachableToast: String?, containView: UIView?) -> Bool {
        if DocsNetStateMonitor.shared.isReachable {
            return false
        } else {
            if let notReachableToastLo = notReachableToast {
                if let containView = containView {
                    UDToast.showFailure(with: notReachableToastLo, on: containView)
                } else {
                    spaceAssertionFailure("cannot get containView")
                }
            }
            return true
        }
    }
     */
}
